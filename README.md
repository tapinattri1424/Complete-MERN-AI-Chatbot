# Complete-MERN-AI-Chatbot
n AI-powered chatbot into a MERN application using a provider-agnostic Node.js (Express) backend that can call either OpenAI APIs or Dialogflow. Expose secure chat endpoints that preserve per-user/session context, support streamed responses (SSE or WebSocket), and enforce token/rate limits.
